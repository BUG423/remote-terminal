{
  "name": "claude-web-agent",
  "version": "1.0.0",
  "description": "Local agent for Claude Web — bridges cloud server to local Claude Code",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "https-proxy-agent": "^9.1.0",
    "node-pty": "^1.1.0",
    "ws": "^8.18.0"
  }
}
